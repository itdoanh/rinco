# Minimal NATS config for development
port: 4222
monitor_port: 8222
http_port: 8222

# JetStream for persistence
jetstream {
  store_dir: /data/jetstream
  max_memory_store: 256MB
  max_file_store: 1GB
}

# Logging
debug: false
trace: false
logtime: true

# General
server_name: "rinco-nats"
